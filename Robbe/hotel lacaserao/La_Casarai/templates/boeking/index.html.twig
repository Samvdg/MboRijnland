{% extends 'base.html.twig' %}

{% block title %}Boeking index{% endblock %}

{% block body %}
    <h1>Boeking index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Start</th>
                <th>End</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for boeking in boekings %}
            <tr>
                <td>{{ boeking.id }}</td>
                <td>{{ boeking.start ? boeking.start|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ boeking.end ? boeking.end|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('boeking_show', {'id': boeking.id}) }}">show</a>
                    <a href="{{ path('boeking_edit', {'id': boeking.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('boeking_new') }}">Create new</a>

    {{ boeking }}
{% endblock %}
