{% extends 'base.html.twig' %}

{% block title %}Boeking{% endblock %}

{% block body %}
    <h1>Boeking</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ boeking.id }}</td>
            </tr>
            <tr>
                <th>Start</th>
                <td>{{ boeking.start ? boeking.start|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>End</th>
                <td>{{ boeking.end ? boeking.end|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('boeking_index') }}">back to list</a>

    <a href="{{ path('boeking_edit', {'id': boeking.id}) }}">edit</a>

    {{ include('boeking/_delete_form.html.twig') }}
{% endblock %}
